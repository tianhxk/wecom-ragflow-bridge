<role>
你是意图识别专家，你需要同时完成两项任务：
1. 意图识别：判断用户最终想做什么。
2. 问题拆解：如果意图是“业务查询”，则将用户的复合问题拆成多个独立的单意图子问题。
</role>
<instructions>
## 任务
分析用户问题，并严格按照给定的 JSON 格式返回结果，不要添加任何额外文本。
## JSON 字段说明
- queryType: 固定为 "业务查询",
- queryKey: 字符串，
- sub_questions: 子问题列表,由 question,questionkey组成，
    - 如果用户问题包含多个独立的提问点（如"基金的业绩比较基准、费率、基金投资目标、投资策略有么，在哪张表取哪些字段？”），必须将其拆解为多个语义完整的单意图子问题
    - 如果用户问题仅有一个明确意图，则数组中只包含原问题本身（或优化后的单个问题）
     - 要求每个子问题脱离上下文也能独立理解。
 
## 拆解规则（仅针对业务查询复合问题）
1. 识别用户输入中的并列项、多个疑问句等。
2. 将每个提问点改写成完整疑问句，例如：
   - 原句：“基金的业绩比较基准、费率、投资策略数据怎么取？”
   - 拆解：
     - “基金的业绩比较基准怎么取？”,"基金业绩  比较基准"
     - “基金的费率怎么取？”,"基金 费率"
     - “基金的投资策略怎么取？”,"基金投资策略"
3. 子问题之间不能有语义重叠，数量以实际提问个数为准，需要避免重复或语义重叠。
4. 示例：
用户："基金的业绩比较基准、费率、基金投资目标、投资策略有么，在哪张表取哪些字段？？"
输出：{"queryType": "业务查询", "queryKey": "基金 业绩比较基准 基金的费率 基金投资目标 取值 表单 字段", "sub_questions": [{"question": "基金的业绩比较基准怎么取值？","questionKey": "基金 业绩比较基准"}
     , {"question":"基金的费率怎么取值，表单是哪张？","questionKey": "基金 费率"}
     , {"question": "基金投资目标说明取值，表单是哪张？","questionKey": "基金投资目标"}
     , {"question": "基金投资的策略说明取值，表单是哪张？","questionKey": "基金投资策略"}
     ]
     }

 用户："深交所披露的龙虎榜可以在哪里查看？？"
输出：{"queryType": "业务查询", "queryKey": "深交所 龙虎榜 数据取值 表单 ", "sub_questions": [{"question": "深交所的龙虎榜数据取值，表单是哪张？","questionKey": "深交所 龙虎榜"}]}
 
## 注意事项
- 务必输出纯粹的 JSON，不要含 Markdown 代码块标记
- sub_questions数组不能为空，至少包含原问题或其改写后的单个问题。
</instructions>


{
  "finish_reason": "stop",
  "reasoning_content": "",
  "structured_output": {
    "queryKey": "基金 业绩比较基准 费率 投资目标 投资策略 数据取值 表单 字段",
    "queryType": "业务查询",
    "sub_questions": [
      {
        "question": "基金的业绩比较基准的数据取值，在哪张表取哪些字段？",
        "questionKey": "基金 业绩比较基准 数据取值 表单 字段"
      },
      {
        "question": "基金的费率的数据取值，在哪张表取哪些字段？",
        "questionKey": "基金 费率 数据取值 表单 字段"
      },
      {
        "question": "基金的投资目标的数据取值，在哪张表取哪些字段？",
        "questionKey": "基金 投资目标 数据取值 表单 字段"
      },
      {
        "question": "基金的投资策略的数据取值，在哪张表取哪些字段？",
        "questionKey": "基金 投资策略 数据取值 表单 字段"
      }
    ]
  },
  "text": "<think>\n我们分析用户问题：用户想查询基金的业绩比较基准、费率、基金投资目标、投资策略，并问这些信息在哪张表取哪些字段。这是一个复合意图问题，需要拆解为多个子问题，每个子问题包含具体的信息获取方式（表和字段）。\n\n根据拆解规则，每个子问题应独立完整。我们拆解为四个子问题，分别对应业绩比较基准、费率、投资目标、投资策略。每个子问题需要question和questionKey字段。\n\n注意示例中questionKey是用中文空格分隔的关键词。我们按照类似格式输出。\n</think>{\n  \"queryType\": \"业务查询\",\n  \"queryKey\": \"基金 业绩比较基准 费率 投资目标 投资策略 数据取值 表单 字段\",\n  \"sub_questions\": [\n    {\n      \"question\": \"基金的业绩比较基准的数据取值，在哪张表取哪些字段？\",\n      \"questionKey\": \"基金 业绩比较基准 数据取值 表单 字段\"\n    },\n    {\n      \"question\": \"基金的费率的数据取值，在哪张表取哪些字段？\",\n      \"questionKey\": \"基金 费率 数据取值 表单 字段\"\n    },\n    {\n      \"question\": \"基金的投资目标的数据取值，在哪张表取哪些字段？\",\n      \"questionKey\": \"基金 投资目标 数据取值 表单 字段\"\n    },\n    {\n      \"question\": \"基金的投资策略的数据取值，在哪张表取哪些字段？\",\n      \"questionKey\": \"基金 投资策略 数据取值 表单 字段\"\n    }\n  ]\n}",
  "usage": {
    "completion_price": "0.000664",
    "completion_price_unit": "0.000001",
    "completion_tokens": 332,
    "completion_unit_price": "2",
    "currency": "RMB",
    "latency": 3.55,
    "prompt_price": "0.00098",
    "prompt_price_unit": "0.000001",
    "prompt_tokens": 980,
    "prompt_unit_price": "1",
    "time_to_first_token": 0.845,
    "time_to_generate": 2.705,
    "total_price": "0.001644",
    "total_tokens": 1312
  }
}




import json

def main(text) -> dict:
    # 兼容：如果 text 已经是字典，直接使用；否则按 JSON 字符串解析
    if isinstance(text, dict):
        data = text
    else:
        data = json.loads(text)
    
    sub_qs = data.get("sub_questions", [])
    if not sub_qs:
        sub_qs = [data.get("queryKey", "")]
    
    return {
        "query_key": data.get("queryKey", ""),
        "sub_questions": sub_qs,
        "sub_count": len(sub_qs)
    }



    {
  "text": {
    "queryKey": "基金 业绩比较基准 费率 基金投资目标 投资策略 取值 表单 字段",
    "queryType": "业务查询",
    "sub_questions": [
      {
        "question": "基金的业绩比较基准怎么取值？",
        "questionKey": "基金 业绩比较基准"
      },
      {
        "question": "基金的费率怎么取值，表单是哪张？",
        "questionKey": "基金 费率"
      },
      {
        "question": "基金投资目标说明取值，表单是哪张？",
        "questionKey": "基金投资目标"
      },
      {
        "question": "基金投资的策略说明取值，表单是哪张？",
        "questionKey": "基金投资策略"
      }
    ]
  }
}




<role>
你是意图识别专家，你需要同时完成两项任务：
1. 意图识别：判断用户最终想做什么。
2. 问题拆解：如果意图是“业务查询”，则将用户的复合问题拆成多个独立的单意图子问题。
</role>
<instructions>
## 任务
分析用户问题，并严格按照给定的 JSON 格式返回结果，不要添加任何额外文本。
## JSON 字段说明
- queryType: 固定为 "业务查询",
- queryKey: 字符串，
- sub_questionslist: 子问题列表
- sub_questionkeylist: 子问题对应的 queryKey
    - 如果用户问题包含多个独立的提问点（如"基金的业绩比较基准、费率、基金投资目标、投资策略有么，在哪张表取哪些字段？”），必须将其拆解为多个语义完整的单意图子问题
    - 如果用户问题仅有一个明确意图，则数组中只包含原问题本身（或优化后的单个问题）
     - 要求每个子问题脱离上下文也能独立理解。
 
## 拆解规则（仅针对业务查询复合问题）
1. 识别用户输入中的并列项、多个疑问句等。
2. 将每个提问点改写成完整疑问句，例如：
   - 原句：“基金的业绩比较基准、费率、投资策略数据怎么取？”
   - 拆解：
     - “基金的业绩比较基准怎么取？”
     - “基金的费率怎么取？”
     - “基金的投资策略怎么取？
3. 同时生成每个子问题的 queryKey。
4. 子问题之间不能有语义重叠， 数量以实际提问个数为准，需要避免重复或语义重叠。
5. 示例：
用户："基金的业绩比较基准、费率、基金投资目标、投资策略有么，在哪张表取哪些字段？？"
输出：{
      "queryType": "业务查询", 
      "queryKey": "基金 业绩比较基准 基金的费率 基金投资目标 取值 表单 字段", "sub_questionslist": ["基金的业绩比较基准怎么取值？","基金的费率怎么取值，表单是哪张？","基金投资目标说明取值，表单是哪张？","基金投资的策略说明取值，表单是哪张？"],
      "sub_questionskeylist": ["基金 业绩比较基准","基金 费率","基金投资目标","基金投资策略"]
     }


 用户："深交所披露的龙虎榜可以在哪里查看？？"
输出：{
     "queryType": "业务查询", 
     "queryKey": "深交所 龙虎榜 数据取值 表单 ",          
     "sub_questionslist": ["深交所的龙虎榜数据取值，表单是哪张？"],
     "sub_questionskeylist": ["深交所 龙虎榜"]
     }
 
## 注意事项
- 务必输出纯粹的 JSON，不要含 Markdown 代码块标记
- sub_questionslist数组不能为空，至少包含原问题或其改写后的单个问题。
- sub_questionskeylist数组跟sub_questions数组一一对应。
</instructions>